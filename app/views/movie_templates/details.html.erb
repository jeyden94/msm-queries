<h1>Movie #<%= @the_movie.id %> details</h1>

<a href="/movies">Go back</a>

<hr>
<% Movie.all.each do |a_movie| %>
<dl>
  <dt>Title</dt>
  <dd><%= @the_movie.title %></dd>

  <dt>Year</dt>
  <dd><%= @the_movie.year %></dd>

  <dt>Duration</dt>
  <dd><%= @the_movie.duration %></dd>

  <dt>Description</dt>
  <dd><%= @the_movie.description %></dd>

  <dt>Image</dt>h
    <dd><img src="<%= @the_movie.image %>" width="200"></dd>

  <dt>Director</dt>
  <dd>
      <% matching_records = Director.where({ :id => a_movie.director_id}) %>
      <% the_director = matching_records.at(0) %>

      <%= the_director.name %>
    </td>
  </dd>

  <dt>Created at</dt>
  <dd><%= @the_movie.created_at %></dd>

  <dt>Updated at</dt>
  <dd><%= @the_movie.updated_at %></dd>
</dl>
<% end %>
